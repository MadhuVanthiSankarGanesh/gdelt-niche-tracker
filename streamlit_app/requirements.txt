pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=0.24.2
plotly>=5.0.0
matplotlib>=3.4.2
seaborn>=0.11.1
# wordcloud is not compatible with Python 3.13, comment or remove the next line if using Python 3.13+
# wordcloud==1.9.2
textblob>=0.17.1
nltk>=3.6.0
streamlit>=1.22.0
boto3>=1.17.0
python-dotenv>=0.19.0
requests>=2.25.1
botocore>=1.20.0
watchdog>=2.1.0
tqdm>=4.62.0
s3fs>=2021.8.0

# AWS Lambda/Serverless (if needed)
botocore>=1.20.0

# For compatibility with Streamlit Cloud
watchdog>=2.1.0

# Other (for robust parsing, etc.)
tqdm>=4.62.0

# If you use uuid, json, or datetime, these are standard library and do not need to be listed

# If you use s3fs for pandas read/write to S3 (optional)
s3fs>=2021.8.0
# If you use uuid, json, or datetime, these are standard library and do not need to be listed

# If you use s3fs for pandas read/write to S3 (optional)
s3fs>=2021.8.0


